Summary:
The hashing function hashes a value, if used in combination with hex it can output a color code.

How to Open:
Double click on the executable (the.exe).

Controls:
The Hash function can be used simply by enterring the following into main
	unsigned int varName = HashFunction::defaultHash("[String]", [Number of lettings in the string]);